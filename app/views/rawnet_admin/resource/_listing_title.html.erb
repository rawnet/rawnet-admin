<h2>Manage <%= resource_class.to_s.titleize.pluralize %></h2>

<%= link_to "Add #{resource_class.to_s.titleize}", [:new, resource_class.to_s.underscore], { class: 'btn btn-primary pull-right' } if controller.respond_to?(:new) %>